{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "bluesky-mcp-server",
  "version": "0.4.0",
  "description": "Search posts, profiles, feeds, threads, and trending topics on Bluesky via MCP. STDIO or Streamable HTTP.",
  "author": {
    "name": "cyanheads",
    "email": "casey@caseyjhand.com",
    "url": "https://github.com/cyanheads/bluesky-mcp-server#readme"
  },
  "homepage": "https://github.com/cyanheads/bluesky-mcp-server#readme",
  "repository": "git+https://github.com/cyanheads/bluesky-mcp-server.git",
  "license": "Apache-2.0",
  "keywords": ["bluesky", "atproto", "social-media", "mcp", "mcp-server", "model-context-protocol"],
  "mcpServers": {
    "bluesky-mcp-server": {
      "command": "npx",
      "args": ["-y", "@cyanheads/bluesky-mcp-server"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio",
        "BLUESKY_IDENTIFIER": "${user_config.BLUESKY_IDENTIFIER}",
        "BLUESKY_APP_PASSWORD": "${user_config.BLUESKY_APP_PASSWORD}"
      }
    }
  },
  "userConfig": {
    "BLUESKY_IDENTIFIER": {
      "type": "string",
      "title": "Bluesky handle (for post search)",
      "description": "Optional. The account post search runs as — use a dedicated one. Leave blank (with the app password) and every other tool still works; only post search is left out.",
      "default": ""
    },
    "BLUESKY_APP_PASSWORD": {
      "type": "string",
      "title": "Bluesky app password (for post search)",
      "description": "Optional. A standard, non-privileged app password for that account (Settings → Privacy and security → App passwords). Set it together with the handle.",
      "sensitive": true,
      "default": ""
    }
  }
}
